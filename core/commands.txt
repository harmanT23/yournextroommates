# Grapph Models visualizations

# Apps with Grouping
./manage.py graph_models -a -g -o ynr_visualized.png

# Certain Models
./manage.py graph_models -a -I User,Listing,Gallery,GalleryImage -o key_models.png

# Arrow Notation
./manage.py graph_models -a --arrow-shape normal -o ynr_arrow_not.png